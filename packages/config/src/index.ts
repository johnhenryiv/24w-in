/**
 * Shared configuration exports
 */

export const brandTokens = {
  accent: 'var(--brand)',
  accentDark: '#e8006b',
  accentLight: '#c1005a',
} as const;

export const fontFamilies = {
  display: 'var(--font-display)',
  body: 'var(--font-body)',
  bodyA11y: 'var(--font-body-a11y)',
  mono: 'var(--font-mono)',
} as const;
