{
  "name": "@24w/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration — ESLint, TypeScript, adherence rules",
  "exports": {
    "./oxlint": "./_adherence.oxlintrc.json"
  },
  "files": [
    "src",
    "*.json"
  ],
  "scripts": {
    "build": "echo 'config package (reference only)'"
  }
}
